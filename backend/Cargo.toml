[package]
name = "backend"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }

# (Cross-platform audio I/O)
cpal = "0.15"

anyhow = "1.0"

log = "0.4"
env_logger = "0.10"